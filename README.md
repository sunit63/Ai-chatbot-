# Ai-chatbot-
This project is a smart AI system that includes a self-learning chatbot and a spam detection model. The chatbot learns from user input and stores data in a JSON file, while the spam detection system classifies messages using machine learning techniques like TF-IDF and Naive Bayes.
